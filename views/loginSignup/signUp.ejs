<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <link rel="stylesheet" href="/css/signUp.css">
    <link rel="icon" href="/images/logo.png">
    <link rel="stylesheet" href="/css/navbar.css">
</head>
<body>
    <%- include('../partials/navbar') %> 
    <div class="signUpContainer">
        <div class="signUpText">
            <h1>Kayıt Ol</h1>
            <p>Zaten bir hesabınız var mı? <a href="/login">Giriş Yap</a></p>
        </div>
        <div class="signUpForm">
            <% if (successMessage && successMessage.length > 0) { %>
                <div style="background-color: #d4edda; color: #155724; padding: 10px; margin-bottom: 15px; border-radius: 5px; text-align: center;">
                    <%= successMessage %>
                </div>
            <% } %>

            <% if (errorMessage && errorMessage.length > 0) { %>
                <div style="background-color: #f8d7da; color: #721c24; padding: 10px; margin-bottom: 15px; border-radius: 5px; text-align: center;">
                    <%= errorMessage %>
                </div>
            <% } %>
            
            <form action="/signup" method="post" class="signUpForm">
                <input type="text" placeholder="Name" name="userName" class="nameInput">

                <input type="text" placeholder="Surname" name="userSurname" class="surnameInput">

                <input type="email" placeholder="Email" name="userEmail" class="emailInput">

                <input type="password" placeholder="Password" name="userPassword" class="passwordInput">

                <button type="submit" class="signUpButtonInput">Sign Up</button>
            </form>
        </div>
    </div>
</body>
</html>